# Reports — p53-mdm2

End-to-end multi-scale p53–MDM2 demonstration with OpenUSD as the intermediary tying MD to systems-biology (MaBoSS) modelling. See `__threads__/p53-mdm2/INTENT.md` for the brief.

## Round 00 — cycle-000 (2026-07-08)

- [00-architecture_v0.md](00-architecture_v0.md) — Architecture analysis: the v8→p53_mdm2 **reuse map** (reuse-as-is / generalize / greenfield / leave-behind per asset), external-input decisions (1YCR starting structure, 5-node `p53_Mdm2` MaBoSS model, DDMut-PPI ΔG API), generalized contracts, risk register, and a 5-milestone roadmap sketch.

## Round 01–03 — cycle-001 (2026-07-10, planning/design)

- [01-md_reproducibility_survey_v0.md](01-md_reproducibility_survey_v0.md) — SOTA survey for sharing MD setup parameters; identifies MDDB (EU Molecular Dynamics Data Bank) and recommends a ~14-attribute `bio:md:` schema grounded in the ShinobuLab GENESIS decks.
- [02-dg_maboss_correlation_v0.md](02-dg_maboss_correlation_v0.md) — ΔG↔MaBoSS-parameter correlation design: governing knob, logistic ΔΔG→S mapping + closed-form inverse, anti-tautology round-trip test.
- [03-cycle001_findings_v0.md](03-cycle001_findings_v0.md) — cycle-001 synthesis + decisions (roadmap authored, topic-split rationale, Q-003/Q-004 filed).

## Round 04 — cycle-002 (2026-07-13, first code cycle)

- [04-cycle002_findings_v0.md](04-cycle002_findings_v0.md) — Pipeline 1 (MD→USD topology) landed: committed 1YCR topology `.usda` (`/p53_MDM2_complex`, 2 chains, 818 atoms), 9/9 falsification-resistant read-back tests, 2 clean anti-chimera gates; PI Q-003/Q-004 folded into the roadmap (p1b unblocked + promoted to critical-path).

## Round 05–06 — cycle-003 (2026-07-14, Pipeline 2 + p1b schema)

- [05-cluster_md_recon_v0.md](05-cluster_md_recon_v0.md) — knowledge-transfer: read-only recon of dgx1/banyan for the self-run MD track. Key finding — the clusters support **Singularity, not unprivileged Docker** (contradicts the Q-003 assumption); both were unreachable this cycle (local rsync blocker / missing banyan config) so all facts are doc-sourced pending live verification. **Superseded by report 07** (live-verified). Scoped, PI-gated plan for p1b Step 2.
- [06-cycle003_findings_v0.md](06-cycle003_findings_v0.md) — Pipeline 2 (OpenUSD→MD ΔG) landed: p53-peptide `Genotype` VariantSet (off ABL T315I) + rate-limited ddMut-PPI client writing ΔΔG back as provenance-tagged `bio:` attrs; live submit worked but retrieval 500'd → honest `unavailable` (no fabricated ΔG), fixture path for the reviewable artifact. p1b Step 1 seeded: `bio:md:` schema (17 CORE incl. PI-promoted ion-conc + protonation, 7 optional) on a Protocol-layer prim. 17/17 checks pass. Q-005 filed (Docker→Singularity pivot).

## Round 07–08 — cycle-004 (2026-07-21, Pipeline 3 emit + cluster live-verify)

- [07-cluster_liveverify_v1.md](07-cluster_liveverify_v1.md) — knowledge-transfer (v1, supersedes 05): **LIVE-verified** dgx1/banyan recon after the PI's Q-005 config fixes. Tooling unblocked; both clusters reachable. **Key correction:** banyan's unprivileged Docker *works* (user in `docker` group — reverses 05's inference), dgx1's does not; Singularity present on both (dgx1 3.5.2 / banyan 4.2.2). Docker→Singularity pivot **confirmed** on portability + Slurm-integration grounds. Hardware/scheduler facts all live-confirmed. Container build/convert/stage/submit enumerated as PI-gated.
- [08-cycle004_findings_v0.md](08-cycle004_findings_v0.md) — Pipeline 3 (OpenUSD→MaBoSS emit) landed: R02 ΔG↔param logistic correlation + logit inverse (`dg_correlation.py`) and a text-templating emitter (`emit_model.py`) producing byte-identical `.bnd` + `$KMn_pMCD`/`$KMn_pMC`-substituted `.cfg` per mutant, with `bio:maboss:*` write-back (inverse reconstructable from USD). Reference matches R02 exactly. 28/28 checks pass; usdchecker exit 0. p1b Step 2 substrate live-verified (report 07). Q-005 effectively resolved.

## Round 09 — cycle-005 (2026-07-24, Pipeline 4 read-back + GROMACS scaffold)

- [09-cycle005_findings_v0.md](09-cycle005_findings_v0.md) — Pipeline 4 (MaBoSS→OpenUSD read-back) landed → **all 4 pipelines now committed + tested**. `run_maboss.py` drives a **real MaBoSS 2.6.6 run** (external colomoto binary; the in-process `cmaboss` backend was flaky and distrusted); `build_analysis_layer.py` writes node-probability trajectories as time-sampled `bio:maboss:prob:<node>` onto a separate analysis SubLayer (base topology untouched). Deferred directional test now real and **passes**: W23A time-avg P(p53↑) 0.396 > WT 0.310 (correct sign, destabilization-monotone). 31/31 checks; verifier verdict **aligned**. Also: non-mutating **GROMACS Singularity scaffold** for p1b Step 2 (`cluster/`, PI chose GROMACS) — nothing built/uploaded/submitted; every cluster mutation stays PI-gated.

## Round 10 — cycle-006 (2026-07-27, read-only cluster state refresh)

- [10-cluster_state_refresh_v0.md](10-cluster_state_refresh_v0.md) — observation: strictly READ-ONLY refresh of banyan/dgx1 state, structured as a delta against report 07. **Build-blocker correction:** report 07's (and the runbook's) claim that banyan has `--fakeroot` is **false** — user `eliott` has no `/etc/subuid`/`/etc/subgid` mapping on *either* cluster, so `singularity build` from a `%post`-bearing `.def` (Route A) cannot run; **Route B** (docker daemon on banyan → `docker-archive://`) is the open path. **Also changed:** banyan GPU 0 now holds another user's 86 GB vLLM process while Slurm still reports the node idle (unscheduled use → a `--gres=gpu:1` job may land on the busy card); banyan root disk down 147 G. **Newly recorded:** CPU/SIMD inventory — Xeon Gold 6530 (banyan) / 6130 (dgx1), **AVX-512 on both** — this closes the *last* remaining `[assumption]` in the sibling-revised `gromacs.def`, whose own note blamed report 07 for having "NO `lscpu` output"; least-common-denominator is dgx1's Skylake-SP core AVX-512 set; the rsync ≥ 3.0 answer (`/opt/homebrew/bin/rsync` 3.4.4 *locally* — already first on PATH; clusters ship 3.2.7, never the problem); **outbound internet works on both, no proxy**; and two `gromacs.def` `[assumption]`s verified true (the `nvidia/cuda:12.9.1-devel-ubuntu22.04` tag exists; the GROMACS 2025.3 tarball URL returns 200). Unchanged: singularity 3.5.2/4.2.2 skew, drivers, GPU inventory, Slurm versions, empty queues, shared 13 T home, no `~/p53mdm2/` or `.sif`, no MD engine. Closes with a "before you build" block. **Nothing was written, built, uploaded, or submitted on either cluster.**

## Round 11 — cycle-006 (2026-07-27, P5 integrated demo + live ΔΔG)

- [11-cycle006_findings_v0.md](11-cycle006_findings_v0.md) — findings: **the four pipelines plus the P5 integrated demonstration now run end-to-end on REAL third-party ΔΔG data**, composed on one stage, **39/39** checks, `usdchecker` RC=0 → per INTENT's Done definition the topic's headline deliverable is essentially met (p1b Step 2 — the actual cluster MD run — still has not run at all). **The three-cycle "ddMut-PPI outage" was our own client bug:** retrieval needs `job_id` in a *multipart form body*; the query-string form returns HTTP 500. Proved side-by-side (`8e94864`, `encoding_diagnostic/`). **Live data changed the biology story:** ΔΔG F19A −3.917 / L26A −2.948 / W23A −6.192 (vs fixture −2.8 / −1.9 / −3.9); ordering held with *no* threshold retuning (WT 0.310 < L26A 0.326 < F19A 0.398 < **W23A 0.861**) — but W23A's S=0.008 sits deep in the logistic's flat tail (dΔΔG/dS ≈ 81 vs 4.1 at F19A), so **0.861 is partly a saturation artifact of the unfitted `m=−3.0, k=1.5`**, flagged as the weakest scientific link. **The authorized cluster build did not happen** — the async harness's permission classifier refused the mutating dispatch twice, so the gate is now *tooling*, not authorization (**Q-006**); the recon that ran instead **invalidated the runbook's recommended route** (no `/etc/subuid` mapping ⇒ Route A very likely dead; Route B promoted, `Dockerfile` written) (**Q-007**). Also: `GMX_SIMD=AVX2_256` now `[verified]` (the GROMACS docs *reversed* the naive least-common-denominator conclusion); two latent build-breakers caught in `gromacs.def` (apt CMake 3.22 vs required ≥3.28; jammy-vs-noble gcc); cycle-005's fixture figures corrected (L26A **0.313384**, F19A **0.322379**); and a cross-track collision in the shared worktree that briefly corrupted committed evidence.

## Round 12 — attended PI session (2026-07-30, roadmap re-founding)

- [12-roadmap_migration_audit_v0.md](12-roadmap_migration_audit_v0.md) — observation: the `__roadmap__/p53-mdm2/` campaign was found **unmutable** — all 8 of its files fail `dirtree-rdm validate`, so every status update and amendment aborts on pre-flight, which is why cycle-006's completed `p4`/`p5` were never marked done. **The decisive finding is that a grammar repair would not have been enough:** `dirtree-rdm update` resolves nodes by matching the Mermaid ID against the filesystem stem, and **6 of 7 IDs are abbreviations** (`p1_topology` vs `p1_topology_from_1ycr`, `p1b_mdparams` vs `p1b_md_parameter_representation`, …), so the staleness was unfixable in place regardless. Also diagnosed: the campaign's BNF-forbidden sibling `-->` arrows were a *symptom of a modelling error*, since the grammar treats siblings as parallel and a flat 7-leaf graph therefore asserts all seven pipelines run at once. Per PI direction the campaign was **re-founded** as `__roadmap__/p53-mdm2-v2/` rather than patched: all 7 leaves carried with **step counts preserved exactly (2,3,2,2,2,2,2 = 15)** and 14 of 15 step titles and commit messages byte-identical; `p4_maboss_readback` and `p5_integrated_demo` **corrected to done** per R11; `p1b` Step 2 reframed as a delegation to the new `p1b_container_runtime/` subtree, with its false "dgx1/banyan have no Singularity" premise marked **superseded and quoted** rather than deleted. New campaign: **17/17 files valid, 9/9 IDs matching stems.** Discloses the grammar-required fields that had to be *invented* (all 15 `Consistency Checks`, 13 `Implementation Logic`, 8 step-level `Goal`) since invented content is what a reviewer must scrutinise. **Retirement of the old campaign is withheld pending PI sign-off of this audit**, so a failed review costs nothing.

## Round 13 — p1b_container_runtime, Step 1 (2026-07-30, Route B build observation of record)

- [13-route_b_build_observed_v0.md](13-route_b_build_observed_v0.md) — observation: files the 2026-07-29 attended Route B build as a citable report rather than leaving it in the reports-README prose above. **Slurm job 30 COMPLETED, exit 0, 5m27s**, `gromacs-p53mdm2:latest` (`70659e395c53`, 10.6 GB), independently corroborated by the `/home/eliott/p53mdm2/BUILD_STATUS` sentinel (`stage=docker_build exit=0`). **Both tarball integrity gates passed against the real fetched bytes** — this is the first time the GROMACS sha256 (previously only cross-corroborated between Spack and EasyBuild, never vendor-published) was checked against the actual tarball, and it matched. The in-image build-time gate printed a real GROMACS 2025.3 / CUDA / AVX2_256 version block. Three more `[assumption]`s close out: **docker data-root observed** at `/var/lib/docker` (on `/`, client cannot redirect it); banyan's `/` held **439 G free across four further days**, reframing report 10's 147 G drawdown as a one-off rather than an ongoing trend (the `TMPDIR`/`SINGULARITY_TMPDIR` advice stays, since `/tmp` is confirmed on the same device as `/`); and the **`libcuda.so.1` DT_NEEDED question resolves** — the build's own CPU-only `RUN gmx --version` gate passed with `CUDA driver: 0.0`, precisely the no-driver-present signature, so the gate does not hard-fail without a GPU. **Route A is now dead by observation, not inference**: the exact `FATAL --remote, --fakeroot, or the proot command are required` error was captured, and both `/etc/subuid`/`/etc/subgid` (zero `eliott` entries) and `proot` (absent from `PATH`) close the two hatches the error names; `--remote` is declined on principle. Confidence is stated per finding rather than blanket, since R10's own front-matter conflated the two. **Still not true at R13's writing, and since closed or narrowed by the sibling leaves (2026-07-31):** no GPU had run the container, no `.sif` existed, `sm_70`/`sm_90` SASS was unverified, and cleanup had not happened — job 32 put GROMACS on a real H100, job 33 delivered and verified `gromacs.sif` (sha256 `1fc04f8b…d20c81ac`) which also opens under dgx1's singularity 3.5.2, and the SASS audit records `SM_ELF=sm_70;sm_90`; **still open** are a dgx1 *GPU* run, cold-cache build reproducibility, and the last ~10 GB of build scratch. See `examples/p53_mdm2/cluster/README.md` and `examples/p53_mdm2/cluster/evidence/`.

## Round 14 — cycle-007 (2026-07-31, container evidence under test + cross-cluster byte parity)

- [14-cycle007_findings_v0.md](14-cycle007_findings_v0.md) — findings: the container campaign's **cluster evidence is now under test in the repo**, and stage-once-run-anywhere is **observed byte-for-byte** instead of asserted from matching `df` output. **48/48** checks (up from 39), via a new `container-evidence` layer of 9 rows over `cluster/evidence/` + both recipe files; the layer contributes **0** rows when `evidence/` is absent, since `run_tests.py` has no skip concept and reads `passed` as a bool. Its highest-value gate is **not** the SASS list but **recipe↔evidence consistency** — five pins parsed out of BOTH `Dockerfile` and `gromacs.def`, asserted to agree with each other and with the captured output, mechanising a sync obligation the runbook concedes is only social. **First genuine test of cross-cluster portability:** one sha256 `1fc04f8b…d20c81ac` computed independently from banyan and from dgx1 agrees (same NFSv4 export `ts2:/export/home` from `10.5.1.206`, identical size and nanosecond mtime), and a **singularity-ce 4.2.2 image `inspect`s (rc 0) and `exec`s (rc 0) under dgx1's singularity 3.5.2** — the version-skew risk open since R07 **did not materialise**, and the mechanism is squashfs 4.0 / **gzip** / 128 KiB blocks, i.e. the writer never reached for zstd or lz4. **Caveat kept in the risk register: gzip is a builder *default*, not a contracted guarantee.** The leaf's prediction that `gmx --version` would fail on `libcuda.so.1` was **wrong, benignly** — exit 0 with `CUDA driver: 0.0`, consistent with `LIBCUDA_DT_NEEDED=no` — and is **not** evidence the image can drive a dgx1 GPU. **Success-gate audit:** 20 gates over four leaves, **15 ticked** (glyph-only, `dirtree-rdm validate` rc=0 throughout), and **3 of the 5 non-met are gate-text defects rather than missing work** — `sass_portability_audit` g2 is **falsified** (`SM_PTX=sm_70;sm_90` with `PTX_RECORDS == ELF_RECORDS == 98`, and **no `compute_*` token** in the evidence to disambiguate the benign per-cubin reading → **Q-008**); `convert_verify_cleanup` g1 **over-claims** a "first build-reproducibility datum" the manifest verbatim disclaims; `convert_verify_cleanup` g5 is a **leaf/parent honesty divergence** — the PI reworded the cleanup gate in the parent at `9be52cb` to record it NOT met, but the leaf was never reworded and its weaker wording *is* satisfied, so a reader of the leaf alone would tick it (→ **Q-010**). **Runbook truth repair:** never updated after jobs 32/33, it asserted in its top banner and six further places that `gromacs.sif` does not exist, that no GPU has executed the container, and that the SASS targets are under audit — all three false; repaired with the dated-changelog idiom preserved (cycle-006 entries untouched, the 2026-07-30 clauses labelled *superseded* rather than deleted). Also: **zero permission refusals this cycle**, a direct contrast with cycle-006's two, but the dgx1 `fs_checksum` MCP tool failed with `could not communicate with process` (an endpoint crash, **not** a refusal; `sha256sum` fallback recorded in the manifest) — Q-003 beta-feedback. Process finding, **second cycle running**: three sub-agents in one git worktree collided twice (one commit swept in another unit's staged files, split by soft-reset — `71bb6dd` discarded, `a46515b` real, no files touched on disk; and a zero-rows test renamed `evidence/` aside while two siblings were reading it) → concurrent sub-agents need `isolation: worktree` or serialising. **Still not true, and said plainly:** no dgx1 GPU has run the image, build equivalence was measured against a **cached** compile layer (`#7`/`#8 CACHED`) so it is **not** a cold-cache reproduction, the cleanup gate is **not met** (last ~10 GB needs `docker image prune` over a store of 52 dangling images), and **no p53-MDM2 MD simulation has ever run on any cluster** — every execution so far is a smoke-test SPC water box. **Q-009** asks the next direction: p1b's unattended-safe work is exhausted.

## Round 15 — cycle-008 (2026-08-13, results-consumption boundary + both cluster runtimes moved)

- [15-cycle008_findings_v0.md](15-cycle008_findings_v0.md) **(latest)** — findings: **53/53** checks (up from 48), and the cycle's real news is that **both cluster container runtimes moved to `singularity-ce 4.5.0-noble` while nobody was looking**. The PI's brief opened by instructing `CHECK 'ssh banyan' BEFORE planning any banyan work`; doing exactly that showed banyan **back up** on kernel 6.8.0-137, its 22→24 upgrade landed hours earlier — so the two items recorded as "STILL BLOCKED, BOTH NEEDING BANYAN" were no longer blocked by reachability. **Two PI hypotheses falsified, in opposite directions.** (1) The gzip guard is **live**: banyan's writer moved 4.2.2 → **4.5.0-noble**, exactly as predicted — but the 4.5.0 default squashfs compressor is **undetermined read-only** (no key in `singularity.conf`, none in `--help`/man, no env var, and binary strings never place a compressor name adjacent to the `-comp` literal), and was recorded as undetermined rather than inferred from the version number; settling it needs one attended build. Mitigation is already on disk — `/opt/singularity/4.2.2` survived and `module load singularity/4.2.2` still resolves — so this is a check to run, not a risk to fear. (2) The hypothesis that a rebuilt `/var/lib/docker` would take the 52 dangling images and 16.46 GB build cache with it and make the next rebuild **cold for free** is **falsified**: the cache survived at the *identical* 16.46 GB / 108 records with entries dated "4 months ago", so the cold-cache gate still needs an explicit `--no-cache`. **Unpredicted, and it outranks both:** dgx1's default singularity **also** moved to 4.5.0 — job 31 printed it while job 30, from the *identical script line on dgx1* fourteen hours earlier, printed 3.5.2, both captures committed. The campaign's headline cross-cluster claim (*a 2019 runtime mounts a 2024-written image*, manifest seq 5) is now about a reader that is **no longer dgx1's default**; the skew may have closed by both ends moving, which retires the risk *and* the evidence together (→ **Q-011**). **The last open gate in `container-runtime-verification` is closed:** dgx1 job 31 (exit 0, 1m16s, **zero harness refusals**, submitted via the sanctioned skill) ran a `-pme cpu` arm and the 4 PME-path JIT entries **vanish** — so they belong to the PME/cuFFT path, **not `libgromacs`**, and nothing in `libgromacs` JIT-compiles on the V100 across the whole min+md path. The zero is a measurement rather than a broken cache **because a 13-file forced-JIT control fired in the same environment**; arms A/B/C reproduced job 30 exactly, same counts *and* byte totals; the real `~/.nv/ComputeCache` was 192K before and after. The new check **recomputes the verdict label from the counts** instead of hard-coding 4, so a re-capture must move its label with its numbers. **Roadmap rollup defect found and fixed:** `dgx1_sif_open_check` carried **4/4 gates ✅ on the leaf while all three parent READMEs showed 0/4 and the node `⬜ Planned`** — the Q-010 leaf/parent divergence *mirrored*, parents under-reading where the leaf over-read. Seven gates rolled up, each verified against the **evidence file** rather than the child's tick with file:line cited, two node statuses advanced via `dirtree-rdm update` only, `validate` rc=0 on all eight files. **Two gates were deliberately left unticked**: one is *literally* satisfied but only by the weaker wording the PI declared defective at leaf level, and one carries the same over-claim the PI split out a day earlier — both escalated as **Q-012** rather than ticked. Also **a structural tooling hole**: Progress tables are BNF-managed so CLAUDE.md forbids hand-editing them, but `dirtree-rdm` exposes **no command to write them**, so they can only ever go stale — and two demonstrably are. Beta-tooling: dgx1 `fs_checksum` **still** fails with `could not communicate with process`, **thirteen days** unfixed, on a path banyan's plugin checksums fine. Trap recorded: `module` is now a login-shell *function* on banyan and reports `not found` under plain `ssh banyan '<cmd>'`, so a unit probing that way would wrongly conclude the cluster has no modules. **Unchanged and still the campaign's real gap: no p53-MDM2 MD simulation has ever run on any cluster** — every execution to date, on either machine, is a 2652-atom smoke-test water box.

- [15-results_consumption_boundary_v0.md](15-results_consumption_boundary_v0.md) — architecture: a **bounded** answer to the PI's 2026-07-29 INBOX workstream (1), "we should also be able to see the visuals from the simulation and not only the numbers", which the PI raised together with its own rabbit-hole warning. **Three grounding facts had to be established by reading the code, and one contradicts the question's premise:** the PI's "50k samples" are **not** in the USD file — `sample_count = 50000` is the Monte Carlo trajectory count, carried as a single scalar `bio:maboss:sampleCount`, while what the Analysis layer actually stores is 4 variants × 5 nodes × 500 frames = 10 000 floats in **310 KB** of ASCII; what is stored is therefore already a **reduction** (`get_nodes_probtraj()`, the marginal over Boolean states), not raw data; and the genuinely raw output — `*_probtraj.csv`, `*_fp.csv`, `*_statdist.csv` — is written by MaBoSS into a `tempfile.mkdtemp` and **discarded**, so the payload candidate does not exist on disk anywhere. **"Store the plots as payloads" is rejected as malformed, not merely unwise:** a payload arc targets scene description — `SdfPayload` takes an asset path **plus a prim path** — so it can never point at a PNG (context7-verified); and the arc exists for deferred loading, which 310 KB does not need. **The boundary rule, which is the deliverable:** *USD stores what a read-back test can assert against the observables; anything a consumer serialises into bytes USD cannot verify — image, PDF, pickled figure — stays outside, and USD carries at most a regeneration command.* It cuts the infinite producers/consumers regress at the **file-format** boundary rather than by enumerating tools, and its stated cost is that **it excludes the artifact the PI asked for** — a committed PNG is byte-for-byte indistinguishable from a stale one to every tool in this repo, a failure class this campaign has never carried. **Three consequences, all of them implementing the design doc rather than revising it:** author §4.1's never-written **Review** department carrying the observables as linear `UsdGeomBasisCurves` + a `Camera`, which clears the PI's own stated bar (vanilla `usdview`), scrubs with the molecule, and stays assertable vertex-by-vertex against the time samples; put the discarded **state-space** trajectory behind the campaign's **first exercised Payload arc**, which also buys hop 4 its only independent oracle (marginalisation must reproduce the stored node probabilities); and generate MaBoSS's own figures **outside** USD. **pyMaBoSS: reuse it, and the reason is better than expected** — `maboss.StoredResult(path, prefix)` is a pure post-hoc file reader that runs no simulation and never touches the distrusted `cmaboss` backend, so it plots standalone-binary output directly, and `run_maboss.py` already does `import maboss`, so **the dependency cost is zero** and the footprint objection dissolves; the honest reason the figures stay out of USD is the opacity rule alone. Five verified caveats recorded (plot methods return `None` and never `savefig`; two helpers leak to the pyplot current axes; `StoredResult.palette` starts empty so the same Boolean state can be drawn in different colours **across variants**, which would actively mislead the four-variant comparison; nodes never ON are silently absent; `plot_observed_graph` raises `AttributeError` on a `StoredResult` and needs undeclared graphviz). **INTENT correction:** the repository it names, `sysbio-curie/pyMaBoSS`, **404s and does not redirect** — the canonical repo is `colomoto/pyMaBoSS`. **Stated as unverified:** that `StoredResult` round-trips against real MaBoSS **2.6.6** output (the parser was read, nothing was executed against a 2.6.6 file), and the payload layers' eventual size. Roadmap handoff authored as `__roadmap__/p53-mdm2-v2/p6_results_consumption/` — 3 leaves over 2 levels, 8 steps, `dirtree-rdm validate` rc=0 on all six files; depth encodes the one real dependency (`StoredResult` cannot read a run directory that does not exist) and no verification tooling, per the PI's ruling that it lives in the separate top-level campaign. **No pipeline code was written** — this cycle scopes the workstream. Four decisions are escalated to the PI, the first being whether the opacity rule stands at all.

## Status

**Update 2026-08-13 (cycle-008, unattended) — the live state.** **53/53** checks. Both cluster container runtimes have moved to **`singularity-ce 4.5.0-noble`**: banyan's writer from 4.2.2 (its 22→24 upgrade landed ~00:50 JST, and banyan is **reachable again**, contradicting the 2026-08-12 brief), and — unpredicted — **dgx1's reader from 3.5.2**, evidenced by two committed captures from the identical script line fourteen hours apart. The campaign's cross-cluster portability claim was therefore earned against a reader that is **no longer dgx1's default**, and the skew may have closed by both ends moving (**Q-011**). The 4.5.0 default squashfs compressor is **undetermined read-only** and was recorded as such rather than inferred; `/opt/singularity/4.2.2` survives on banyan and its module still resolves, so a rebuild can pin the original writer. **The cold-cache gate is NOT freed** — `/var/lib/docker` survived the OS upgrade with its build cache byte-identical at 16.46 GB / 108 records, falsifying the hypothesis that a rebuild would come cold for free. **`container-runtime-verification` is fully closed**: dgx1 job 31 (exit 0, zero harness refusals) showed the 4 PME-path JIT entries **vanish** under `-pme cpu` against a 13-file forced-JIT control, so they are **PME/cuFFT, not `libgromacs`** — nothing in `libgromacs` JITs on the V100. Roadmap rollup was found **silently stale in the parent direction** (leaf 4/4 ✅, all three parents 0/4 and node `Planned`); seven gates rolled up against evidence files, two node statuses advanced via `dirtree-rdm`, and two gates deliberately **left unticked** and escalated because ticking them would re-encode wordings the PI had already ruled defective (**Q-012**). Workstream (1) is scoped: the **opacity rule** and three new `p6_results_consumption/` leaves (**Q-013**). **Still not true, and said plainly:** the cold-cache rebuild has not happened, the cleanup prune gate is **not met**, and **no p53-MDM2 MD simulation has ever run on any cluster** — every execution to date is a 2652-atom smoke-test water box. Q-011 … Q-014 await answers via `umbod questions`. See [findings v0](15-cycle008_findings_v0.md).

---

Cycle-006 complete — **all four pipelines + the P5 integrated demonstration run end-to-end on live DDMut-PPI ΔΔG**, composed in a thin 82-line root layer with no copied pipeline data; **39/39** read-back checks (7 new integrated checks falsification-tested against 8 deliberately corrupted stages); `usdchecker` RC=0. INTENT's Done definition is essentially met at the pipeline level. **Still open:** p1b Step 2 (a real cluster MD run) has not executed — blocked by the async harness rather than by the PI (**Q-006**), with the container route itself changed under it (**Q-007**); and the demo's headline P(p53↑) depends on unfitted correlation constants `m=−3.0, k=1.5` whose fitting is a PI-owned scientific choice. Two soft questions await answers via `umbod questions`. See [findings v0 steering questions](11-cycle006_findings_v0.md).

**Update 2026-07-30 (attended PI session, outside the cycle machinery).** Q-006 and Q-007 are **answered** and cycle-006 is **acked** (`pi-reviewed`, 0 open questions). Q-006: container builds stay PI-attended — the path now proven to work — while other clean, engineering-justified work should be *attempted* unattended and escalated through the umbod question path rather than stalled; no pre-allow rule was added. Q-007: **Route B confirmed by observation, not inference** — an attended probe made `singularity build` fail in seconds (`FATAL: --remote, --fakeroot, or the proot command are required…`), `/etc/subuid` and `/etc/subgid` carry zero mappings, and `proot` is absent, closing the third hatch singularity itself names. **Route B then succeeded on the first attempt:** Slurm job 30 built a 10.6 GB image on banyan in 5m27s, exit 0, reporting GROMACS 2025.3 / `GPU support: CUDA` / `SIMD: AVX2_256` / CUDA runtime 12.90. Two further `[assumption]`s resolved: banyan's docker data-root **is** `/var/lib/docker` (on `/`, client cannot redirect it), and `/` held steady at 439 G free across four days, so R10's 147 G drawdown was a **one-off, not a leak**. **As recorded on that date and superseded within two days — still not true *then*:** no GPU had yet run the container (`CUDA driver: 0.0`), no `.sif` existed, the `sm_70`/`sm_90` SASS targets were unverified, and the build scratch was not reclaimed — those were the four leaves of `p1b_container_runtime/`. See the 2026-07-31 update below, which is the live state. Roadmap work is recorded in [R12](12-roadmap_migration_audit_v0.md); the old campaign awaits sign-off before retirement.

**Update 2026-07-31 (cycle-007, unattended).** Three of those four clauses are now **false**: attended jobs 32/33 put GROMACS on a real banyan **H100** (`CUDA driver: 13.20`, compute cap 9.0) and delivered `/home/eliott/p53mdm2/gromacs.sif` (sha256 `1fc04f8b…d20c81ac`, 5750255616 bytes), the SASS audit records `SM_ELF=sm_70;sm_90`, and the build scratch is reclaimed apart from the last ~10 GB. This cycle then added the repo-side and read-only half: **48/48** checks via a new `container-evidence` layer, and the image observed **byte-identical on both clusters** and **opening under dgx1's singularity 3.5.2**. **Still not true, and not claimed anywhere above:** no **dgx1** GPU has run the image; build equivalence was measured against a **cached** compile layer, so it is **not** a cold-cache reproduction; the cleanup gate asking `/` to return to its pre-work free space is **not met**; and **no p53-MDM2 MD simulation has ever run on any cluster** — every execution so far is a smoke-test SPC water box. Q-008/Q-009/Q-010 await answers via `umbod questions`. See [findings v0](14-cycle007_findings_v0.md).
