# Questions

## Q-001 p53-MDM2 input data: where do the topology/trajectory files live, and what format? USDBIO_DATA_DIR was unset in the agent shell. Pipeline 1 extraction assumes a PDB+XTC shape like v8's; if the system ships mmCIF or a non-mdtraj trajectory, pdb_parser/xtc_to_clips need more than parameterization. If no p53-MDM2 MD data exists yet, should cycle-001 start from the 1YCR crystal structure alone (topology-only USD) until a trajectory is available?
*asked: cycle-0*
*priority: soft*
*answer:* There are no trajectories yet. But is it necessary for our purpose? Isn't the only MD data we need the dG for a demonstration of how to use MD data to inject into higher-level compartment-based models and simulations. That being the case, you have access to both dgx1 and banyan. I know we haven't installed the software for MD there (I think...) but it is worth considering including MD simulations that you could also manage. In that case, we need to be even more careful about which crystal structure to chose from. And the experimentation parameters to run the MD simulations would also need to become part of the USDBio intermediate representation (although that's also a green field, I think since we didn't need that part before). If we do that, we should likely have subagents survey how the SOTA is sharing these parameters for reproducibility. I heard from Shinobu-san there is a database of MD trajectories fundede by Europe research. Maybe their format is a good start?
Regarding  `tools/patch_stage_metadata.py` in pipeline 1. Based on your comment that this was necessary only because of the pxr and mdtraj issue, shouldn't we drop it from the transfer? You indicate to generalize but that's already a solved problem, no?
Pipeline 2 connects to the comments above. In fact, exploring this topic specifically might warrant a dedicated umbod topic on its own. — PI, 2026-07-09
## Q-002 dG binarization threshold (Pipeline 3): what ddG (kcal/mol) cutoff flips the p53-MDM2 complex from bound to unbound, and should a destabilizing variant flip Mdm2N.istate=FALSE only, or also force the node OFF? Pipeline 3 will take the threshold as a parameter regardless, but the demo needs a defensible default.
*asked: cycle-0*
*priority: soft*
*answer:* This connects to my answer to Q-001. In the first place I think you misunderstood the fact that we don't know which dG value to use. and I won't be able to give you a defensible value. However, we can rely on linking MD simulations with the dG results we get from the server to infer that behavior. The other solution, potentially complementary is to do an inverse exploration from the MaBoSS models. Because the values of the hill functions in the MaBoSS `bnet` files are completely adhoc. They are tuned only to fit the observation data. In our present case, we don'at have a choice but to choose the hill function values that correlates with the dG and the point is that the dG is the inverse of that correlation function. This, of course, assumes the model is correct. But this this is a tutorial model anyway, and let's not forget the real purpose is to push the limits of the MD + MaboSS link through our potential USDBio intermediate representation, not to discuss the validity of the model.
And these discussions are ultimately linked to the depth of the mandatory implementation and components of pipeline 3 and 4. Again, it might be worth seeding immediately dedicated umbod topics on them rather than trying to handle everything here. Of course, if you manage your subagents perfectly, then there is no problem for you to lead the exploration directly in THIS topic. That would save us a lot of communication overhead between the topics. — PI, 2026-07-09

## Q-003 Q-003 — Self-run-MD decision: should the project run its OWN p53-MDM2 MD simulations on dgx1/banyan (per your Q-001 idea), which makes the MD setup parameters a critical-path USDBio representation concern (roadmap node p1b, now Blocked on this decision)? Or stay ΔG-only for the demonstration — take ΔΔG from the ddMut-PPI server and treat p1b as deferred/optional? Context: no MD software is currently installed on dgx1/banyan (you noted this), and cycle-001's survey (report 01) identified MDDB (EU Molecular Dynamics Data Bank) as your referenced European DB and designed a ~14-attribute bio:md: schema grounded in the real ShinobuLab GENESIS decks, ready to build if the answer is yes. Sub-question: should ion concentration and protonation state (not derivable from geometry) be promoted into the bio:md: CORE set, or left optional?
*asked: cycle-1*
*priority: soft*
*answer:* 
Yes, we promote p1b and make sure the project runs its own p53-mdm2 MD simulation. The reason is that it gives us a real use case for leveraging this connection to banyan/dgx1. Indeed we can consider it to still be in beta and so we should tread carefully and use the knowledge-report format as instructed in the skill /writing-reports to document how to improve the usage patterns and experience for the working agent. This also means you must be careful for any mutating commands or installation commands as this is a shared resource.
Regarding the lack of MD simulation software, this is also a good opportunity to stress test the connection. We have two ways: either we instal everything directly on the server. Or we containerize things. On dgx1 and banyan, I don't think there is singularity: we rather use docker. It will likely take some work to set up and optimize the container, but the environment would then be reusable for future MD simulations. I suppose the pattern is simply to use the container for execution and bind-mount the folder with to get input data and save output.
Promote ion concentration and protonation to the core. — PI, 2026-07-12
## Q-004 Q-004 — Topic-split confirmation: you offered a choice (Q-001/Q-002) between seeding dedicated umbod topics for pipelines 2/3/4 vs. leading the exploration directly in THIS topic if sub-agents are well-managed. I have decided to LEAD ALL FOUR PIPELINES HERE, because they are coupled through the single shared USDBio representation (the topic's whole thesis), so splitting would fragment the representation design and add the cross-topic communication overhead you wanted to avoid; cycle-001 ran two parallel design sub-agents to absorb the breadth. This is a soft question: confirm this, or tell me to split off one or more pipelines into their own topics if you'd rather isolate them.
*asked: cycle-1*
*priority: soft*
*answer:* I support your choice. — PI, 2026-07-12

## Q-005 p1b Step 2 execution-model pivot (from cluster recon, report 05): the dgx1/banyan docs say the SUPPORTED container runtime is Singularity, NOT unprivileged Docker (which usually needs docker-group=root on a shared node); banyan even loads Singularity 4.2.2 as a default module, and the submit_job tooling already wraps commands as 'singularity exec --nv'. Your Q-003 directive assumed Docker+bind-mounts. Recommendation: keep Docker as the local BUILD format but RUN on-cluster via Singularity (docker build locally -> convert to .sif -> singularity exec --nv with bind-mounts). Confirm this pivot, or insist on Docker? NOTE: this recon was doc-sourced only -- both clusters were unreachable this cycle (local rsync 2.6.9<3.0.0 blocker on dgx1; banyan ~/.banyan/config.json missing), so no live 'docker info'/'nvidia-smi' was possible. Next cycle must first unblock tooling locally, then run the read-only live-verification pass in report 05 sec.B before any cluster-mutating build/submit (which will be gated on your approval per the beta/shared caution).
*asked: cycle-3*
*priority: soft*
*answer:* 
**Config things**:
1. I added the `.banyan/config.json` file so you shouldn't have issues with that anymore
2. `rsync` >= 3.0  IS installed but you have to search and adapt the PATH for it.
**Docker vs. Singularity**: I have for a fact used and deployed docker containers on `banyan` before. But if your interpretation of the stack leads to using Singularity for integration with the job system, we follow your technical findings. — PI, 2026-07-20

## Q-006 Cluster-mutating steps are authorized by you but BLOCKED BY THE ASYNC HARNESS, not by umbod. Your INBOX 2026-07-25T07:58 authorized runbook gated steps 1-3 (native singularity build on banyan, GROMACS 2025.3 / CUDA 12.9, then 1-GPU smoke test). I dispatched that work twice this cycle and BOTH dispatches were refused by Claude Code's auto-mode permission classifier, which gates cluster-mutating actions in unattended sessions regardless of your written authorization in the thread — the authorization lives in project files, which the harness classifier does not read as permission. So cycle-006 executed only the non-mutating half: a read-only live state refresh of both clusters, and resolving the [assumption]-tagged values in cluster/gromacs.def (CUDA base tag, GROMACS 2025.3 tarball URL + sha256) so the build is turnkey. NOTHING was built, uploaded, or submitted; that remains true. DECISION NEEDED — pick one: (a) run gated steps 1-3 yourself in an ATTENDED Claude Code session where you can approve the prompts interactively (recommended; the runbook in examples/p53_mdm2/cluster/README.md is now build-ready); (b) add a permission rule to .claude/settings.local.json pre-allowing the banyan/dgx1 MCP hpc tools (and any Bash they need) so future unattended cycles can execute the build you already authorized — note this durably widens what unattended sessions may do on the shared clusters; or (c) keep the build permanently PI-attended by policy and let async cycles only ever do read-only cluster work. This is soft: the rest of the cycle proceeded (P5 integrated demo, live ddMut-PPI retry), but p1b Step 2 cannot advance past scaffolding until you choose.
*asked: cycle-6*
*priority: soft*
*answer:* DECISION: build runs stay ATTENDED for now, because that is the path now proven to work. On 2026-07-29 an attended session built the GROMACS image on banyan via Route B: Slurm job 30, exit 0, 5m27s, image 10.6 GB. Everything else that is clean and strongly supported by engineering rationale should be ATTEMPTED unattended; if the harness refuses, escalate to the PI through the umbod question path rather than stalling or silently substituting other work. No pre-allow rule is being added to the local settings file. Note also that the cycle-006 refusal was partly self-inflicted: the agent leaned on the generic run_command_on_cluster escape hatch instead of the banyan-submitting-jobs and banyan-monitoring-jobs skills, which are the sanctioned route and the one that worked today. Job submission has worked before on other clusters, so submission itself was never the blocker. — PI, 2026-07-29
## Q-007 Route A of the container runbook (your 'native singularity on banyan first' instruction) is very likely INFEASIBLE — technical finding, needs your call on the fallback. This cycle's read-only cluster refresh (report 10, commit c8f2cc2) proved that /etc/subuid and /etc/subgid carry NO mapping for your user on EITHER cluster (banyan has only user/test; dgx1 only lxd/root), even though unprivileged userns is otherwise enabled. 'singularity build' from a .def file needs root-in-container to execute its %post section, so the recommended Route A will almost certainly fail. Caveat: this is inference from the missing subuid mapping, NOT an observed build failure — one attended 'singularity build' attempt would confirm it in seconds. What IS available: you are in banyan's docker group and the daemon answers (v29.4.3, 51 images), so Route B is live — build a Docker image on banyan, 'docker save' it, then 'singularity build gromacs.sif docker-archive://...' and RUN under singularity as planned. Your Q-005 answer already delegated this class of choice to me ('we follow your technical findings'), and I have accordingly promoted Route B to recommended in cluster/README.md — but I am surfacing it because your latest authorization named native singularity specifically, and because Route B means a Docker build on a shared node, which your own Q-003 caution flags as the thing to keep short and off the GPUs. Confirm Route B, or tell me to have an attended session try Route A once to settle it empirically. Related operational findings you should see: (1) banyan GPU 0 currently holds another user's ~86GB vLLM process while Slurm reports the node IDLE with empty AllocTRES — Slurm's view and reality diverge, so any smoke test needs an nvidia-smi pre-flight; (2) banyan's root disk lost 147GB in 6 days (586G->439G free) and /tmp is on root, so build scratch must go to shared home. This is soft — nothing on the clusters is blocked-by-me right now, it is blocked by Q-006 (harness permissions) anyway.
*asked: cycle-6*
*priority: soft*
*answer:* CONFIRMED: Route B, by observation rather than inference. An attended probe on 2026-07-29 ran singularity build from gromacs.def as a non-root user on banyan; it failed in seconds with FATAL: --remote, --fakeroot, or the proot command are required to build this source as a non-root user. A count of matching lines in /etc/subuid and /etc/subgid returns 0 and 0, confirming report 10 exactly. Of the three hatches singularity itself names: --fakeroot needs the absent subuid range and only an admin can grant it; proot is NOT installed on banyan; --remote is declined because it would ship the recipe to a third-party cloud builder. Route A is therefore dead, and report 10 was right to demote it. Route B then succeeded on the first attempt. Two further assumptions resolved in the same pass: the banyan docker data-root IS /var/lib/docker, i.e. on the root filesystem, and the client cannot redirect it; and the root filesystem has held steady at 439 G free across four days, so the 147 G drawdown reported in report 10 was a one-off event, not an ongoing leak. — PI, 2026-07-29

## Q-008 Q-008 -- FALSIFIED SUCCESS GATE in sass_portability_audit, needs your wording or an attended re-capture. The gate predicts 'cuobjdump -lptx shows sm_90 only, matching GMX_CUDA_TARGET_COMPUTE=90, recorded as the intended asymmetry'. The captured evidence contradicts it: SM_PTX=sm_70;sm_90 with PTX_RECORDS == ELF_RECORDS == 98 [source: examples/p53_mdm2/cluster/evidence/sass_audit_banyan.txt]. A plausible benign reading is that 'cuobjdump -lptx' names each PTX record after its ENCLOSING CUBIN, so the sm_70 token is the container's arch rather than the PTX's own target -- and sass_audit.sh greps sm_[0-9]+|compute_[0-9]+ over raw text, which would pick that filename token up. But that is INFERENCE: the capture contains no compute_* token at all to disambiguate, so I did not promote it to a finding and deliberately encoded NOTHING about SM_PTX in the new test module (encoding the gate's prediction would go red; encoding the observation would bless a possibly-stale gate). Pick one: (a) reword the gate against the observation and I encode the observed behaviour as the gate; (b) an attended session re-captures with output that distinguishes a PTX record's target from its cubin name (one docker run, device-free, ~seconds) and we then encode whichever is true; (c) drop the gate as not worth the cost. This is the only success gate in the container-runtime campaign whose PREDICTION was contradicted rather than merely unverified.
*asked: cycle-7*
*priority: soft*
*answer:* (a) REWORD, and the re-capture was done attended in this session rather than deferred, because the answer changed what the wording should be. Two corrections to the framing first. (1) This was already settled and the leaf simply never heard: both Dockerfile and gromacs.def carry a dated CORRECTED 2026-07-30 block recording that GROMACS' CMake emits PTX for every GMX_CUDA_TARGET_SM entry regardless of GMX_CUDA_TARGET_COMPUTE. Gate 2 was a stale leftover of a falsehood I had already fixed in two files. (2) Option (b) could not be 'one docker run' — the gromacs-p53mdm2 image was DELETED in convert_verify_cleanup step 5 (image rows: 0), so the re-capture had to go through the delivered .sif under singularity exec, which is read-only and therefore inside the Q-006 unattended envelope, not attended-gated at all. Result (evidence/ptx_disambiguation_banyan.txt, manifest seq 6): the discriminator is the record EXTENSION. cuobjdump -lptx names records *.ptx and -lelf names them *.cubin, PTX_CUBIN_TOKENS=0 so no cubin name appears in the -lptx listing at all, and there are 49 sm_70 plus 49 sm_90 PTX records. So a PTX record's sm_ token is its OWN target: the gate's sm_90-only prediction is falsified AND so is R14's enclosing-cubin reading. PTX_COMPUTE_TOKENS=0 also explains the never-found compute_* token — cuobjdump names PTX by sm_NN, so sass_audit.sh's compute_[0-9]+ alternative could never match. Gate 2 reworded against the observation and ticked; encoded as check ptx_targets_observed, asserted on per-architecture record COUNTS plus the extension split rather than on SM_PTX alone, because a set-valued key cannot distinguish 'PTX for both' from 'one arch plus a container name mentioning the other'. Note the finding has a sequel: because PTX now demonstrably exists for sm_70, a working V100 run no longer implies sm_70 SASS was used, which is why I then ran the SASS-vs-JIT probe. — PI, 2026-08-12
## Q-009 Q-009 -- NEXT-CYCLE DIRECTION, because p1b's unattended-safe work is now exhausted. This cycle closed every repo-only and read-only item you flagged: the container-evidence test module (closing the open step in three leaves at once), the dgx1 read-only open check (all 4 gates met), the gate audit, and the runbook truth repair. What REMAINS in p1b_container_runtime is PI-attended by your own Q-006 answer: the dgx1 GPU smoke test, a cold-cache rebuild (the only thing that would turn the cached-layer equivalence into a real reproducibility datum), and the cleanup prune (which needs docker image prune and would touch other users' dangling images). Meanwhile your INBOX opened TWO new workstreams that have no roadmap leaves yet: (1) 2026-07-29T08:42 -- the producer/consumer boundary for MaBoSS output. You noted the 50k-sample/500-frame arrays are machine-readable but not how human scientists look at results, and asked whether MaBoSS's own plotting should be reused and the PLOTS stored as payloads, or whether the raw arrays are the payload. You also flagged the rabbit-hole risk of infinite producers/consumers, which I read as asking for a BOUNDED answer rather than a survey. (2) 2026-07-29T08:51 -- the communication/graphics deliverables: unfolding the LIVERPS-mapping and departmental-layering rationales into talk material, an arc-relationship diagram derived from the USD files run_end_to_end.py actually produces, and an MD-USD-MaBoSS figure showing USD as the true intermediate representation. Which do I take next -- (1), (2), both in one cycle, or do you want an attended session to clear the remaining p1b items first so p1b closes before new ground opens? Note (2) is the one whose value depends on a deadline I do not know: if there is a talk date, say so and I will retro-plan from it.
*asked: cycle-7*
*priority: soft*
*answer:* ATTENDED p1b FIRST — close p1b before opening new ground. Partially executed in this attended session; the remainder is BLOCKED externally, not deferred by choice. DONE: the dgx1 GPU smoke test. Job 28, exit 0 — CUDA driver 13.0 against the 0.0 cycle-007 recorded driverless, Tesla V100-SXM2-16GB at compute cap 7.0 'stat: compatible' from both nvidia-smi and GROMACS' own md.log, 'Using GPU 8x4 nonbonded short-range kernels', PME all on device. Cross-cluster minimisation-energy parity against banyan job 33 on the SAME .sif: 2.62e-06. That closes rung 5 of R14's ladder and is the first exercise of the sm_70 half of GMX_CUDA_TARGET_SM=70;90. Also ran an unplanned follow-up the Q-008 answer made necessary — the SASS-vs-JIT probe (jobs 29/30) — because once PTX exists for sm_70 a working V100 run no longer proves sm_70 SASS was used. Verdict MIXED: nonbonded kernels DO come from embedded SASS (default run JIT-compiled nothing while the CUDA_FORCE_PTX_JIT=1 control wrote 9 files / 13.5 MB, so the negative is meaningful), but the full min+md run wrote 4 entries / 47 kB, so the PME path JITs something. Origin undetermined and deliberately not guessed at — GROMACS reaches PME via cuFFT, which ships its own PTX independently of our flags. BLOCKED: the cold-cache rebuild and the gzip guard. banyan is mid Ubuntu 22->24 upgrade and ssh auth is failing (NOT the 24.04 ssh-rsa deprecation — it still reports OpenSSH 8.9 from 22.04 and the ED25519 key is offered correctly; most consistent explanation is that authorized_keys on the shared NFS home is unreadable from banyan right now). Two consequences: the gzip guard stops being speculative, because post-upgrade banyan may ship a newer singularity-ce whose default compressor is not gzip while dgx1's reader stays at 3.5.2 — the FIRST .sif built after the upgrade must be checked against the dgx1 read path; and the prune question may be moot, since a rebuilt /var/lib/docker would take the 52 dangling images and 16.46 GB cache with it and make the next rebuild cold for free. Data is safe: p53mdm2/ is on the NFS server, and I re-verified the artifact digest, inspect rc 0 and gzip payload from dgx1. NOT YET STARTED: both INBOX workstreams. MaBoSS payload boundary and the talk graphics still have no roadmap leaves, and I did not ask for the talk date — supply it when you pick (2) and the retro-plan can start from it. Byproduct work went to a NEW TOP-LEVEL roadmap, container-runtime-verification/, per your ruling that verification tooling should not pollute the work roadmap. — PI, 2026-08-12
## Q-010 Q-010 -- THREE GATE-TEXT DEFECTS found by this cycle's audit that need YOUR wording, not mine, because each is a question of what the campaign should promise rather than what the code does. (1) convert_verify_cleanup gate 1 OVER-CLAIMS: its observable condition ('rebuilt SASS summary and version block identical to the pre-correction capture') IS met, but its interpretive clause ('giving the campaign its first build-reproducibility datum') is precisely what the evidence disclaims -- job 33 logged #7 CACHED / #8 CACHED and the manifest says verbatim that equivalence was measured against a cached compile layer and is NOT a cold-cache reproduction. I left it unticked rather than convert a cached-layer no-op into a reproducibility claim. Recommend SPLITTING it: tick the identity, and either drop the reproducibility clause or re-scope it to a future cold-cache rebuild. (2) convert_verify_cleanup gate 5 is a LEAF/PARENT HONESTY DIVERGENCE. You reworded the cleanup gate in the PARENT p1b_container_runtime/README.md at 9be52cb to record the original as NOT met. The LEAF was never reworded -- git log shows one commit c4f1d84 and its gate text is byte-identical to today's -- and the leaf's weaker wording ('/ free space is RECORDED before and after') IS satisfied by the evidence. So a reader of the leaf alone would tick it and conclude cleanup was complete. The leaf carries no trace of the not-met finding the parent records; that asymmetry is the defect. (3) docker_gpu_smoke gate 4 requires mdrun's exit 0 to be recorded IN THE MANIFEST; it is substantively true but lives in the body file's sentinel instead. Cheapest fix is adding mdrun_exit: 0 to manifest seq 2, or rewording the gate to 'recorded in the committed evidence'. Related and non-blocking: several leaves' Deliverables lists name files you consolidated during the attended session (docker_gpu_smoke.sbatch, sif_gpu_smoke.sbatch, rebuild_equivalence.txt, sif_build.txt, cleanup.txt) into docker_gpu_smoke.sh, convert_verify.sh and two combined evidence files -- I audited gates against CONTENT not filenames, but those deliverable lists are now stale and will mislead the next reader.
*asked: cycle-7*
*priority: soft*
*answer:* ADOPT ALL THREE, as proposed, plus the deliverables pass. Applied at 2f40e81; gate tally across the four audited leaves now 19 of 21, up from 16 of 20, and dirtree-rdm validate rc=0 on all four leaves and the parent. (1) convert_verify_cleanup g1 SPLIT, as you recommended. The observable half — rebuilt SASS summary and version block identical, EQUIVALENCE_SUMMARY=identical at convert_verify_banyan.txt:54-56 — is ticked. The reproducibility clause becomes its own unticked cold-cache gate. Recorded there, because it changes the sequencing: docker build --no-cache needs NO prune, since it ignores rather than deletes the daemon's 16.46 GB build cache, so this gate is fully independent of g5. Its real cost is disk — another ~10.6 GB image on a filesystem at 53%. Also note the cleanup did NOT clear the build cache as the parent implies: convert_verify_banyan.txt:156-160 shows Build Cache 108 records / 16.46 GB with 0B reclaimable after, i.e. the prune reclaimed 34.3 kB. (2) g5 — the parent's honest text is propagated down to the leaf with a pointer and an explicit do-not-tick-from-the-leaf note. Your original weaker leaf wording WAS satisfied, which is exactly why a leaf-only reader would have ticked it. (3) g4 REWORDED to 'recorded in the committed evidence' rather than editing the manifest. I rejected the manifest-edit option deliberately: sass_portability_audit step 2 states the manifest exists so evidence can be distinguished from an edited copy later, and retro-adding a field to turn a gate green sets exactly the wrong precedent, even though manifest_integrity hashes body files and would not have noticed. Deliverables: six stale filenames corrected (rebuild_equivalence.txt, sif_build.txt, sif_gpu_smoke.sbatch/.txt, cleanup.txt, docker_md.log.txt, docker_gpu_smoke.sbatch), each annotated with what was actually delivered rather than silently rewritten, so the consolidation history stays legible. — PI, 2026-08-12

## Q-011 Q-011 -- BOTH CLUSTER RUNTIMES MOVED, which changes what the gzip guard is even testing. Your INBOX item 8 framed the guard as: banyan (the WRITER) may upgrade to a singularity whose default compressor is not gzip, while dgx1 (the READER) stays at 3.5.2. Half of that is now confirmed and half is falsified. CONFIRMED: banyan is up again (Ubuntu 24.04.4, kernel 6.8.0-137) and now runs singularity-ce 4.5.0-noble from dpkg at /usr/bin/singularity, where 4.2.2 built the delivered image. FALSIFIED: dgx1 is ALSO on 4.5.0-noble now -- job 31 printed it today, while job 30 from the IDENTICAL script line on dgx1 fourteen hours earlier printed 3.5.2; both captures are committed. So the 2019-reader-mounts-2024-image result (manifest seq 5), which is the campaign's headline cross-cluster claim, is now about a reader that is no longer dgx1's default. I did NOT probe whether 3.5.2 is still reachable on dgx1 the way banyan keeps 4.2.2 under /opt. Two further facts bear on your choice. (1) The 4.5.0 default squashfs compressor is UNDETERMINED read-only -- singularity.conf carries no compressor key, build --help and the man page document no default, no SINGULARITYENV var is set, and binary strings never place a compressor name adjacent to the -comp literal. I deliberately did not infer it from the version number. Settling it needs one small build, which is attended-gated. (2) The mitigation is already on disk: /opt/singularity/4.2.2 survived banyan's upgrade and 'module load singularity/4.2.2' still resolves to it, so a rebuild can pin the exact original writer with no reinstall. DECISION NEEDED, pick one: (a) attended one-off -- build a throwaway .sif under 4.5.0 and run unsquashfs -s on it, which settles the compressor question permanently and cheaply; (b) sidestep it -- pin 4.2.2 via the surviving module for every future build and keep the guard as a standing pre-flight check rather than an experiment; or (c) retire the guard as moot on the grounds that both ends now run the same 4.5.0, accepting that we would then have NO evidence for cross-version portability and would be relying on version equality instead. Note (c) is the cheapest and also the one that quietly deletes a claim the campaign has been making. Related: does the seq 5 evidence need an annotation recording that its reader is no longer the default, or does it stand as a dated observation?
*asked: cycle-8*
*priority: soft*
*answer:*

## Q-012 Q-012 -- TWO MORE GATE-TEXT DEFECTS needing your wording, found by a rollup audit and identical in class to the ones you settled in Q-010. The trigger: dgx1_sif_open_check carried 4 of 4 gates ticked on the LEAF while all three parent READMEs above it still showed every gate unticked and the node as Planned, so a reader of the parents concluded nothing had been done. That is your Q-010 leaf/parent divergence pointing the other way -- there the leaf over-read, here the parents under-read. Seven gates are now rolled up, each verified against the EVIDENCE FILE rather than against the child's tick, with file and line cited per gate, and two node statuses advanced via dirtree-rdm update only. Two gates were deliberately LEFT UNTICKED because ticking either would encode something you have already ruled against. (1) sif_delivery/README.md gate 3 reads '...byte-identical to the pre-correction capture -- A BUILD-REPRODUCIBILITY DATUM THIS CAMPAIGN OTHERWISE HAS NONE OF'. The observable half holds (EQUIVALENCE_SUMMARY=identical at convert_verify_banyan.txt:55-56) but the trailing clause is precisely the over-claim you split out of the LEAF on 2026-08-12, since equivalence was measured against #7 CACHED / #8 CACHED. The parent needs the same split you gave the leaf. (2) sif_delivery/README.md gate 5, as literally worded ('no gromacs.tar, no gromacs-p53mdm2 image, and / free space recorded before and after'), IS satisfied by the evidence -- but that is exactly the weaker wording you declared defective at leaf level, and ticking it would reinstate the asymmetry in mirror image. It needs the 'and the reclaimed space is actually released' rewording to match the leaf and the p1b parent. Recommend adopting both, as proposed. SEPARATELY, a tooling gap for your Q-003 beta-feedback mandate, which is a real structural hole rather than a nuisance: Progress tables are BNF-managed so CLAUDE.md forbids hand-editing them, but dirtree-rdm exposes NO command to write them -- so they can only ever go stale, and two are demonstrably stale now (sif_delivery's Progress note says convert_verify_cleanup 'Step 4 parity assertions still open' while docker_sif_version_parity and docker_sif_energy_parity exist at test_container_evidence.py:468,480). Do you want this raised upstream against dirtree-rdm, or should the convention change to allow hand-editing Progress specifically? Also unresolved and left for you: sass_portability_audit looks ready to advance to done by the same evidence logic, but I held it back because this cycle was actively working that same SASS/PTX ground.
*asked: cycle-8*
*priority: soft*
*answer:*

## Q-013 Q-013 -- THE OPACITY RULE, and four decisions under it that are yours not mine. This is the bounded answer to your 2026-07-29T08:42 INBOX question about MaBoSS output, delivered as report 15 plus three roadmap leaves under p6_results_consumption/. FIRST, a premise correction you should see before the answer: the '50k samples neatly stored in the USD file' are NOT in the USD file. sample_count=50000 is the Monte-Carlo trajectory count carried as ONE scalar bio:maboss:sampleCount; what is stored is 4 variants x 5 nodes x 500 frames = 10,000 floats, 310,795 bytes, and that is ALREADY a marginal reduction via get_nodes_probtraj(), not raw data. The genuinely raw output (probtraj/fp/statdist CSVs) goes to a tempfile.mkdtemp and is DISCARDED, so the payload candidate does not exist on disk anywhere today. THE ANSWER: payload = the raw state-space trajectory we currently throw away, since deferring 310 KB buys nothing; plots = out of USD entirely, with observables rendered AS USD geometry in a layer vanilla usdview already draws while matplotlib figures stay byproducts beside the run directory and USD carries only the regeneration command; and the boundary rule -- USD stores what a read-back test can assert against the observables, and anything a consumer serialises into bytes USD cannot verify stays outside. That cuts your infinite-producers regress at the FILE-FORMAT boundary rather than by enumerating tools. Two mechanical constraints, both verified rather than recalled: SdfPayload takes an asset path PLUS a prim path, so a payload arc can never target a PNG; and maboss.StoredResult is a pure post-hoc file reader that never touches the distrusted cmaboss backend, so reusing pyMaBoSS plotting costs ZERO new dependencies -- which dissolved the footprint objection I expected to make and leaves the opacity rule as the only honest reason figures stay out. FOUR DECISIONS I COULD NOT MAKE FOR YOU: (1) DOES THE OPACITY RULE STAND? It forbids committing a rendered plot inside the stage, i.e. it excludes the artifact you asked for. Overruling it is legitimate and only needs saying; the cost is a new failure class, since a stale PNG is byte-for-byte indistinguishable from a fresh one to every tool in this repo. (2) Is the Review layer presentation or annotation? The design doc names it 'annotations, cameras, PI comments' and I put result curves there; if Review is reserved for human commentary, the curves need a sixth department. (3) One committed run directory or every run? I recommend one, on the ddmut_ppi_live precedent. (4) Is the state-space payload worth doing at 5-node scale? It is honestly ceremonial there -- it buys hop 4 its only independent oracle and exercises an arc the LIVERPS thesis claims, but preferring to exercise Payloads against a real MD trajectory would be a defensible call, and that trajectory still does not exist on any machine. Also: INTENT names sysbio-curie/pyMaBoSS, which 404s without redirect; canonical is colomoto/pyMaBoSS.
*asked: cycle-8*
*priority: soft*
*answer:*

## Q-014 Q-014 -- THE TALK DATE, which is now the only thing gating the second INBOX workstream. Your Q-009 answer said you would supply it when you picked workstream (2), the communication/graphics deliverables -- unfolding the LIVERPS-mapping and departmental-layering rationales into talk material, an arc-relationship diagram derived from the USD files run_end_to_end.py actually produces, and an MD-USD-MaBoSS figure showing USD as the true intermediate representation. You did not pick it this cycle, so I took workstream (1) instead, which was fully specified and repo-only. Workstream (2) is now the ONLY item in the topic with no path forward that does not depend on you: p1b's remaining items are attended or blocked, (1) is scoped and leaved, and everything else is downstream of Q-011 or Q-013. If there IS a talk date, give it and I will retro-plan backwards from it, which is the one planning shape this deliverable actually needs -- graphics work expands to fill whatever time it is given, and a date is what bounds it. If there is NO date yet, say so explicitly and I will treat (2) as unbounded-and-deferred rather than re-asking every cycle. A related judgement call I would rather you make than infer: the arc-relationship diagram is described as 'derived from the USD files run_end_to_end.py actually produces', which means it should be GENERATED from the composed stage rather than hand-drawn, so that it cannot go stale the way the runbook did. That is more work up front and is only worth it if the figure will be reused across several talks. Is it a one-off for one talk, or a reusable artifact for the project?
*asked: cycle-8*
*priority: soft*
*answer:*

## Q-015 Q-011-ADDENDUM (post-verifier self-correct, read-only) -- THE PROBE I HAD LEFT UNDONE IS DONE, AND IT REMOVES OPTION (c). Q-011 above said I had not probed whether singularity 3.5.2 is still reachable on dgx1 the way banyan keeps 4.2.2 under /opt. The verifier flagged that as a cheap read-only gap that would sharpen the cycle's own headline, so I ran it: evidence/dgx1_runtime_recheck.txt, manifest seq 11. RESULT: dgx1 has NO 3.5.2 anywhere. No /opt/singularity tree, no /opt/modulefiles, no 'module' command under a login shell, no apptainer, and the only installed package is singularity-ce 4.5.0-noble. This is ASYMMETRIC with banyan, which kept 4.2.2 under /opt with a working modulefile. WHAT IT CHANGES: option (c) in Q-011 -- 'retire the guard as moot, both ends now run 4.5.0' -- is no longer merely a decision to delete a claim; it is IRREVERSIBLE on dgx1. The seq 5 result, a singularity-ce 4.2.2 image opening under a 2019-era 3.5.2 reader, is now a DATED OBSERVATION THAT CANNOT BE REPRODUCED. Nothing is lost that was already captured, and seq 5 stands as a historical fact, but we can never re-run that test or extend it to a new image. So the live question narrows to: do we still care about cross-VERSION portability now that both clusters sit on the same 4.5.0, or was that only ever a proxy for cross-CLUSTER portability, which is now testable by version equality alone? My reading is the latter -- the risk we actually cared about was 'an image built on one machine fails to open on the other', and version skew was the mechanism we feared, not the thing we valued. If you agree, option (b) collapses into good hygiene (pin 4.2.2 on banyan only if we ever need to reproduce an old build bit-for-bit) and option (a) reduces to one attended build purely to learn 4.5.0's default compressor -- still worth doing once, because that number governs whether a FUTURE reader can open what we write today, and that is the durable question. CAVEAT ON THE PROBE: I checked the named paths plus the dpkg database; a 3.5.2 binary installed somewhere unusual and off PATH would not have been found, and I ran no filesystem-wide search.
*asked: cycle-8*
*priority: soft*
*answer:*

