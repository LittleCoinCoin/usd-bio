2026-08-12T07:58:34.012035+00:00
ATTENDED PI SESSION 2026-08-12. Read this before picking the topic up; it changes cluster assumptions and closes three questions.

1. BANYAN IS MID UBUNTU 22->24 UPGRADE AND CURRENTLY UNREACHABLE. ssh fails with 'Permission denied (publickey,password)' and the banyan MCP tools fail with exit 255 (an ssh failure, NOT a permission refusal — do not misread it as the auto-mode classifier). It is NOT the 24.04 ssh-rsa deprecation: banyan still reports OpenSSH_8.9p1 from 22.04 and the ED25519 key is offered correctly. Most consistent explanation, unconfirmed from outside: authorized_keys lives on the shared NFS home and banyan cannot read it while the mount is down. CHECK 'ssh banyan' BEFORE planning any banyan work. Data is safe — p53mdm2/ is on the NFS server 10.5.1.206, not banyan; I re-verified the artifact from dgx1 (digest unchanged, inspect rc 0, payload still gzip).

2. DGX1 IS ALREADY ON UBUNTU 24.04.4 AND THE SIF READ PATH SURVIVED ITS UPGRADE. singularity is still 3.5.2. So half the OS-upgrade risk is retired by observation; the residual risk is banyan as the WRITER.

3. Q-008/Q-009/Q-010 ARE ALL ANSWERED — read them in 'umbod questions --full' rather than re-deriving. Headline: Q-008 was settled by an attended re-capture, and the answer falsified BOTH the gate's prediction AND R14's proposed benign reading. cuobjdump -lptx names records *.ptx while -lelf names them *.cubin, and no cubin name appears in the -lptx listing at all, so a PTX record's sm_ token is its OWN target. PTX really is embedded for both architectures (49 records each).

4. RUNG 5 OF THE R14 LADDER IS CLOSED. A dgx1 V100 executed gromacs.sif — job 28, CUDA driver 13.0, compute cap 7.0, nonbondeds and PME on device, mdrun exit 0, and cross-cluster minimisation-energy parity of 2.62e-06 against banyan job 33 on the SAME image. First exercise of the sm_70 half of GMX_CUDA_TARGET_SM=70;90.

5. NEW, AND IT WEAKENS A HEADLINE CLAIM SLIGHTLY — the image is NOT entirely JIT-free on dgx1. Because Q-008 established PTX exists for sm_70, a working V100 run no longer implies sm_70 SASS was used, so I measured it. Nonbonded kernels DO come from embedded SASS (default run wrote 0 JIT cache entries while a CUDA_FORCE_PTX_JIT=1 positive control wrote 9 files / 13.5 MB, so the negative is meaningful). But the full min+md run wrote 4 entries / 47 kB, so the PME path JIT-compiles something. ORIGIN UNDETERMINED and deliberately not guessed at: GROMACS reaches PME via cuFFT, which ships its own PTX independently of our build flags. This is the ONE open gate in the new roadmap; the discriminator is written down (re-run md with -pme cpu against a fresh cache, ~60s on dgx1, which IS up).

6. BYPRODUCT WORK LIVES IN A NEW TOP-LEVEL ROADMAP, __roadmap__/container-runtime-verification/, per the PI's ruling that verification tooling must not pollute the scientific work roadmap. Two leaves: dgx1_gpu_smoke (done) and sass_vs_jit_provenance (in progress, one open gate). Do NOT fold these back into p53-mdm2-v2.

7. FOUR GATE-TEXT DEFECTS ARE FIXED; the audited tally is now 19 of 21, up from 16 of 20. Both remaining unticked gates are honest not-yet-done work: the cold-cache rebuild and the cleanup prune. IMPORTANT SEQUENCING FACT the parent README implied wrongly — the cleanup did NOT clear the build cache. convert_verify_banyan.txt:156-160 shows Build Cache at 108 records / 16.46 GB with 0B reclaimable AFTER, i.e. the prune reclaimed 34.3 kB. So a plain rebuild would still hit cache; the cold-cache gate needs 'docker build --no-cache', which ignores rather than deletes that cache and therefore needs NO prune and is independent of the cleanup gate.

8. STILL BLOCKED, BOTH NEEDING BANYAN: the cold-cache rebuild and the gzip guard. The gzip guard is no longer speculative — post-upgrade banyan may ship a newer singularity-ce whose default compressor is not gzip while dgx1's reader stays at 3.5.2, so THE FIRST .sif BUILT AFTER THE UPGRADE MUST BE CHECKED AGAINST THE DGX1 READ PATH. Also: the prune question may be moot, since a rebuilt /var/lib/docker would take the 52 dangling images and the 16.46 GB cache with it and make the next rebuild cold for free.

9. SUITE IS 52/52 (was 48/48). Three new checks: dgx1_gpu_executes, crosscluster_energy_parity, sass_not_jit_for_nonbondeds, plus ptx_targets_observed from Q-008. The zero-rows contract and per-family darkness are re-verified by path redirection. GOTCHA: the leaves' documented consistency-check command is incomplete — run_tests.py also needs the OpenUSD lib on PYTHONPATH or it dies with 'pxr not importable'. Use PYTHONPATH=/Users/hacker/Documents/bin/OpenUSD/lib/python:$(pwd)/examples from the repo ROOT.

10. TWO INBOX WORKSTREAMS REMAIN UNSTARTED with no roadmap leaves: the MaBoSS plots-vs-raw-arrays payload boundary, and the talk graphics. The talk date is still unknown — the PI will supply it when he picks that one.

11. UNCHANGED AND STILL THE CAMPAIGN'S REAL GAP: no p53-MDM2 MD simulation has EVER run on any cluster. Everything executed so far, on either cluster, is a 2652-atom smoke-test water box.

12. Two tool notes for the Q-003 beta-feedback mandate. The dgx1 plugin reports '(InvalidAccount)' as a pending reason on a cluster with NO Slurm accounting at all ('sacctmgr show cluster' says no supported accounting_storage plugin), and the rendered sbatch carries no --account; the job ran fine seconds later. Misleading — it invites adding an account the plugin's own skill says to omit. Separately, dgx1 fs_checksum was NOT retried this session; sha256sum over ssh plus a locally verified digest was used throughout, and scp proved byte-faithful where fs_view is not.

