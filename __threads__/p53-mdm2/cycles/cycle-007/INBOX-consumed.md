2026-07-29T08:31:53.513067+00:00
Regarding your callout for the PI's scientific choice, of course in a true scientific project we would have needed to do it and scrutinize the soundness of the ddG<-->S --> KMn_pMC pipeline; this is a serious candidate for a follow up topic but for this I will need to discuss with the scientists that have more knowledge about it. So you can leave it as a footnote like you have already done in the section "Note Done Yet" of the `examples/p53-mdm2/README.md` for now.

2026-07-29T08:42:26.458820+00:00
As soon as GROMACS is online, the pipeline will also integrate the MD simulations. With the previous example `foundation_demo_v8` we showed that we can indeed integrate the MD trajectories as payloads and also visualize them in the vanilla `usdview`. But I wonder what the best comsummer of other simulation types can be. Indeed for now you neatly stored the Maboss 50k samples 500 frames in the usd file: this is a very machine readable format, but that's not how humans scientists are used to see it. I heard Maboss has some prepared plotting features--> is it relevant to reuse them to also store the plots as payloads? Or rather the simulation arrays should be treated as the raw data and become payload...? Basically, to really close the loop, we should also be able to see the visuals from the simulation and not only the numbers. There is a higher level question of what's more on the data production side or more on the data consumption side. to then go back to be stored in USD. It's a dangerous question because we can easily fall into a rabbit whole of infinite producers/consummers.

2026-07-29T08:51:33.358942+00:00
I am starting to think about how to communicate all the work done in this example, the substrate used for the tests (i.e. p53-mdm2 MD + Maboss), what OpenUSD is trully useful for (basically unfolding and verbalizing rationales for the sections `LIVERPS mapping` and `Departmental layering, verified` from `examples/p53_mdm2/README.md`), can we also draw out a diagrams of how all arcs relate to each other in the usd files produced from the `run_end_to_end.py` file, can we draw the MD-USD-Maboss in a way that shows that USD is truly the intermediate representation? And so on. Basically many graphic materials that can be leveraged or inserted in a talk to share and decipher the rich content of this example as well as where it points us for the future of using USD for multiscale modeling.

2026-07-30T17:20:28.577330+00:00
ATTENDED PI SESSION 2026-07-29 to 2026-07-31. p1b Step 2 finally has a real artifact, and the roadmap it lives in was re-founded. Read all of this before picking the topic up.

1. WORK FROM THE NEW CAMPAIGN ONLY. __roadmap__/p53-mdm2-v2/ supersedes __roadmap__/p53-mdm2/, which is retired behind a blocked banner plus MIGRATION.md. Reason: all 8 of its files failed dirtree-rdm validate, and its Mermaid node IDs were abbreviations rather than filesystem stems, so dirtree-rdm update could not resolve 6 of 7 nodes even after a grammar repair. Every node and every step was carried across and audited row-by-row in R12 (__reports__/p53-mdm2/12-roadmap_migration_audit_v0.md), which I signed off before retirement. p4_maboss_readback and p5_integrated_demo are now correctly marked done.

2. DELIVERED. /home/eliott/p53mdm2/gromacs.sif on the shared NFS home, sha256 1fc04f8b48a87f7e0cce4c4b1f3ae7ea5cd640b55c22586c115ce3bed20c81ac, 5750255616 bytes. GROMACS 2025.3 observed executing on a banyan H100 under singularity exec --nv: CUDA driver 13.20, compute cap 9.0, PME and nonbondeds both on device. Both cluster architectures are present as real SASS (SM_ELF=sm_70;sm_90), so the dgx1 leg is viable. Docker-to-sif minimisation-energy parity 1.39e-06. Build scratch reclaimed. Route A is dead by observation, Route B is the path: see the Q-007 answer.

3. WHERE TO RESUME. See __roadmap__/p53-mdm2-v2/p1b_container_runtime/. Three leaves are inprogress and they all converge on ONE missing artifact: examples/p53_mdm2/tests/test_container_evidence.py plus a container-evidence layer registered in run_tests.py. Writing it closes the open step in all three at once. It is repo-only, needs no cluster, and is therefore unattended-safe. HARD CONSTRAINT: the module must return zero rows when examples/p53_mdm2/cluster/evidence/ is absent, because run_tests.py has no skip concept and reads passed as a bool, so anything else makes the suite permanently red. Its highest-value assertion is recipe-versus-evidence consistency: parse the pins out of BOTH Dockerfile and gromacs.def, assert the twins agree with each other and with the captured output. That mechanises a sync obligation the runbook itself admits is only social.

4. ALSO SAFE TO TAKE. dgx1_sif_open_check under sif_delivery/crosscluster_readonly/ is read-only by construction: sha256 parity of the sif computed from both clusters, singularity inspect under dgx1 singularity 3.5.2, and a directory listing. No GPU, no sbatch, no writes. It is the first genuine test of the stage-once-run-anywhere claim, which until now rested only on matching df output.

5. STILL PI-ATTENDED per the Q-006 answer: any container build, and the dgx1 GPU smoke test, which is deliberately out of scope for now.

6. DO NOT OVERSTATE TWO THINGS. Both are recorded in-repo. Build equivalence was measured against a CACHED compile layer, so it proves the label removal changed no compiled content; it is NOT a cold-cache reproduction. And the cleanup gate demanding that / return to its pre-work free space is NOT met and was reworded rather than ticked: releasing the last 10 GB needs docker image prune, which would delete other users dangling images from a store holding 52 of them.

7. TRAPS THAT COST ME TIME, so you do not repeat them. The banyan docker daemon has Default Runtime nvidia and CUDA base images carry NVIDIA_VISIBLE_DEVICES=all, so EVERY docker run sees both H100s with no --gpus flag; pass -e NVIDIA_VISIBLE_DEVICES=void for a genuinely device-free container, and resolve an allocated GPU from SLURM_JOB_GPUS rather than CUDA_VISIBLE_DEVICES, which Slurm may rewrite to allocation-relative indices. fs_view is not byte-faithful, it drops a leading blank line, so always compare fs_checksum against a locally computed digest before trusting captured evidence. And use the banyan-submitting-jobs and banyan-monitoring-jobs skills rather than the generic command runner: that is the sanctioned route and the one that actually worked.

Reports added this session: R12 (roadmap migration audit) and R13 (Route B build observed). Verbatim evidence with a provenance manifest is in examples/p53_mdm2/cluster/evidence/.

